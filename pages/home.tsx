import Calendar from "../components/calendar";

const Home = () => {
  return (
    <div style={{ height: "100vh" }}>
      {/* react-big-calendar */}
      <Calendar />
    </div>
  );
};

export default Home;
